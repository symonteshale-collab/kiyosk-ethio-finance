import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.KIYOSK',
  appName: 'Kiyosk',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
